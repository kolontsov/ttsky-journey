[project]
name = "journey"
version = "0.1.0"
description = "Journey - retrowave ASIC demoscene entry, build scripts"
requires-python = ">=3.10"
dependencies = [
    "Pillow",
    "numpy",
    "sounddevice",
    "Jinja2",
]
